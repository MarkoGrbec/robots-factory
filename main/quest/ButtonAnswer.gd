class_name ButtonAnswer extends Button

func _ready() -> void:
	pass # TODO
	#var tween = create_tween()
	#visible_characters = 0
	#var dialog_speed = text.length() * g_man.misc.slow_writing
	#tween.tween_property(self, "visible_characters", text.length(), dialog_speed)
